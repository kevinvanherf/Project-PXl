@using HogeschoolPXL.Data.DefaultData;
@using HogeschoolPXL.Models.ViewModels.Identity;
@using Microsoft.AspNetCore.Identity;
@inject SignInManager<User> SignInManager

@{
    ViewData["Title"] = "Home Page";
    string search = ViewBag.search;
}

@if (User.IsInRole(Roles.Student))
{
    <form action="/" method="post" class="search" >
        @Html.TextBox("search") <input class="form-control mr-sm-2" type="submit" value="search" type="search" style="width: auto;"/>
    </form>
    <br />
    <h2>Cursussen : </h2>
    <br />
    <Student search="@search" ></Student>

}
@if (User.IsInRole(Roles.Lector))
{
        <form action="/" method="post" class="search" >
        @Html.TextBox("search") <input class="form-control mr-sm-2" type="submit" value="search" type="search" style="width: auto;"/>
        </form>
        <br />
        <h2>Cursussen : </h2>
        <br />
       
    <Lector  search="@search"></Lector>
}
@if (User.IsInRole(Roles.Admin))
{
    <p>kijk bij accounts als er iemand new is die een account nodig heeft </p>
}
@if (User.IsInRole(Roles.NwGebruiker))
{
        <p>
            hello. 
            Je kunt nog nieks doen u moet wachten dat u bent ingeschreven in de school dit kan effe duren. 
            Bedankt voor ons begrip 
        </p>
}


